/**Session storage is similar to cookies, but the data is only stored for the current session. This means that
 * the data will be deleted when the user closes the browser. Session storage is useful for storing data that
 * is sensitive, such as login credentials. */
sessionStorage.setItem("userName","Abhijith Chowdary");
sessionStorage.getItem("userName");